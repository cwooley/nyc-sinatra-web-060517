<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit <%= @figure.name %></title>
  </head>
  <body>
    <form action="/figures/<%= @figure.id %>" method="post">
      <input id="hidden" type="hidden" name="_method" value="patch">
      <label>Name</label><input type="text" name="figure[name]" id="figure_name" value="<%= @figure.name %>">
      <% Title.all.each do |title| %>
        <!-- todo: Make Titles and Landmarks pre checked for existing titles/landmarks -->
          <p><label><%= title.name %></label> <input type="checkbox" name="figure[title_ids][][<%= title.id %>]" id="<%= 'title_' + title.id.to_s%>" value="on"></p>
      <% end %>
      <p><label>New Title</label><input type="text" name="figure[title][name]" id="new_title"></p>
      <% Landmark.all.each do |landmark| %>
        <p><label><%= landmark.name %></label> <input type="checkbox" name="figure[landmark_ids][][<%= landmark.id %>]" id="<%= 'landmark_' + landmark.id.to_s%>"></p>
      <% end %>
      <p><label>New Landmark Name:</label><input type="text" name="figure[landmark][name]" id="new_landmark"></p>
      <p><label>New Landmark Year Completed:</label><input type="text" name="figure[landmark][year_completed]"></p>
      <input type="submit" name="Edit Figure" id="Edit Figure">
    </form>
  </body>
</html>
