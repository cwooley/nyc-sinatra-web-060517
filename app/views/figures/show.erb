<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= @figure.name %></title>
  </head>
  <body>
    <h1> <%= @figure.name %> </h1>
      <ul>
        <% @figure.titles.each do |title| %>
        <li><%= title.name %> </li>
        <% end %>
      </ul>
  </body>
</html>
